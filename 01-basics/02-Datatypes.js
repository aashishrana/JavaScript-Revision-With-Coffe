"use strict"; // treat all js code newer version
// alert(4 + 4); // we are using nodejs not browser

// Data types
/*Number,
BigInt,
String => " "
Boolean,
null => standalone value 
Symbol => unique */

// Object

console.log(typeof "Aashish");
console.log(typeof null);
console.log(typeof (undefined));